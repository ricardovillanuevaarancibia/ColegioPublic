@model ColegioPublic.ViewsModel.UserVM.AddEditUserViewModel
@{
    ViewBag.Title =Model.UserId.HasValue ? "Agregar " : "Editar "+ " Usuario Sistema";
}
<div class="row">
    <div class="col-sm-12">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">@(Model.UserId.HasValue ? "Agregar " : "Editar ")Usuario</h4>
                <form class="form" action="@Url.Action("AddEditUser","User")" method="post">
                    @Html.HiddenFor(x => x.UserId)
                    <div class="form-group mt-5 row">
                        @Html.LabelFor(x => x.UserName, new { @class = "col-2 col-form-label" })

                        <div class="col-10">
                            @Html.TextBoxFor(x => x.UserName, new { @class = "form-control" })
                        </div>
                    </div>
                    <div class="form-group mt-5 row">
                        @Html.LabelFor(x => x.Password, new { @class = "col-2 col-form-label" })

                        <div class="col-10">
                            @Html.TextBoxFor(x => x.Password, new { @class = "form-control" })
                        </div>
                    </div>
                    <div class="form-group mt-5 row">
                        @Html.LabelFor(x => x.Rol, new { @class = "col-2 col-form-label" })

                        <div class="col-10">
                            @Html.TextBoxFor(x => x.Rol, new { @class = "form-control" })
                        </div>
                    </div>
                    <div class="form-actions">
                        <div class="card-body">
                            <button type="submit" class="btn btn-success"> <i class="fa fa-check"></i> Save</button>
                            <button type="button" class="btn btn-dark">Cancel</button>
                        </div>
                    </div>

                </form>
            </div>
        </div>
    </div>
</div>

@section scripts{
    <script type="text/javascript">

        $(function () {

        });
    </script>
}