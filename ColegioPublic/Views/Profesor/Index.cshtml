@model ColegioPublic.ViewsModel.ProfesorVM.IndexProfesorViewModel
@{
    ViewBag.Title = "Lista Profesor";
}

<h2>Lista de Profesores</h2>

@using (Ajax.BeginForm("_ListProfesor", "Profesor", new AjaxOptions
{//GetPatients is name of method in PatientController
    InsertionMode = InsertionMode.Replace, //target element(#patientList) will be replaced
    UpdateTargetId = "paged-section",
    LoadingElementId = "loader" // div with .gif loader - that is shown when data are loading
}))
{
    @Html.LabelFor(x => x.q, new { })
    <div class="input-group">

        @Html.TextBoxFor(x => x.q, new { @class = "form-control" })
        <button class="btn btn-success btn-sm"><i class="fa fa-search" aria-hidden="true"></i></button>
    </div>

}
<hr />
<div class="button-group text-right">
    <a href="@Url.Action("AddEditProfesor","Profesor")" class="btn btn-success"> <i class="fa fa-plus-circle" aria-hidden="true"></i> Nuevo Profesor</a>
</div>

<div class="table-responsive" id="paged-section">
    @{Html.RenderAction("_ListProfesor", Model);}
</div>



